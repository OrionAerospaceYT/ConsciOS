name: ConsciosTest
run-name: ${{ github.actor }} test
on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    # The main unit testing action
    - uses: actions/checkout@v4
      with:
        repositry: adafruit/ci-arduino
        path: ci 
    - name: installer
      run: bash ci/actions_install.sh

